declare module 'lodash.debounce' {
  // Minimal declaration to satisfy TypeScript when @types isn't installed.
  // If you prefer full types, install @types/lodash.debounce as a devDependency.
  const debounce: any;
  export default debounce;
}
